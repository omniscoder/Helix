# Motif Logo

```bash
helix viz motif-logo --input motif.json --save motif.png
```

Renders PWM columns as information logos (bits). Accepts uniform or custom backgrounds; emits spec/provenance files alongside the image.
