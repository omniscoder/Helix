# RNA Dot-Plot

```bash
helix viz rna-dotplot --input ensemble.json --save dotplot.png
```

Visualizes McCaskill pairing posteriors (`P[i,j]`). Upper triangle heatmap + `dotplot.viz.json` + provenance trailer guarantee notebook-to-figure parity.
