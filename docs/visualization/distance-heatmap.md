# Distance Heatmap

```bash
helix viz distance-heatmap --input distances.json --save distances.png
```

Plots MinHash/HLL distance matrices with consistent metadata (labels, min/max/mean). Ideal for quick clustering demos.
